# Day 1 — 2026-07-20

## Done

- 建立 `BrainAgent-fMRI` Git 仓库。
- 对照 `AgentTask.docx` 审查并完善 `AGENTS.md`。
- 明确第一周优先完成 derivative-first 最小垂直路径。
- 明确完整 Planner、持久 Memory 和自动 Recovery 从第 8 天开始实现。
- 建立可执行风险表及风险状态规则。
- 完成 README 草稿和按天独立记录的目录约定。

Evidence:

- `README.md`
- `AGENTS.md`
- `docs/RISKS.md`
- `docs/daily/2026-07-20.md`
- Git branch：`main`
- Initial commit：`cd20008`

## Blocked

- 尚未确定首个可访问的 fMRIPrep derivative visible case。
- 尚未冻结基础模型、图谱、主要依赖及其版本。
- 尚未验证候选基础模型权重和输入格式。
- 尚未形成第 2 天所需的最小 TaskSpec 和 Validator 列表。

已采取的动作：

- 将上述问题登记为 `docs/RISKS.md` 中的开放风险。
- 确定第一周采用 derivative-first，以避免 raw fMRIPrep 路径阻塞 MVP。

## Next

第 2 天唯一的最小目标：

> 创建一个绑定可访问 derivative case 的最小 TaskSpec，并明确 required outputs、success criteria 和关键 Validator 列表。

计划内容：

1. 选择或确认一个小型 fMRIPrep derivative case。
2. 记录 BOLD、confounds、metadata、space 和 TR。
3. 创建 `cases/visible/case_01.yaml` 或等价 TaskSpec。
4. 定义最小 required outputs。
5. 列出 MVP 的关键确定性 Validators。
6. 更新风险表中的 R-02、R-03、R-04、R-05 和 R-08。

## AI collaboration

AI 建议：

- 先使用 derivative-first 路径跑通最小系统。
- 将完整 Planner、Memory 和 Recovery 延后至第 8 天。
- 将风险从静态列表改为包含状态、关闭条件、证据、下一动作和降级方案的可执行表。

本人判断：

- 接受这些建议。它们与 `AgentTask.docx` 中“第一周必须跑通最小垂直路径”和“第 8 天完成 Planner、Memory、Recovery”的阶段安排一致。
- 不在第 1 天提前实现完整 Harness，避免架构工作挤占数据和模型可行性验证时间。

验证证据：

- `AGENTS.md` 中的阶段优先级、MVP 路径和分阶段 Definition of Done。
- `docs/RISKS.md` 中的可执行风险状态。